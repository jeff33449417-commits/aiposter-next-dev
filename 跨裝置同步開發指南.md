# 跨裝置雲端同步開發指南 (Google Drive Developer Workflow)

本專案已遷移至 Google Drive 同步資料夾下。為了確保您在 **Mac 電腦 A** 與 **另一台電腦 B (Mac 或 Windows)** 之間切換時，程式碼與本機開發環境能無縫同步且不發生衝突，請遵循此工作流指南。

---

## 📂 跨平台同步路徑對照

| 裝置類型 | Google Drive 本機掛載路徑 |
| :--- | :--- |
| **Mac 電腦 (File Provider)** | `/Users/jeff/Library/CloudStorage/GoogleDrive-jeff33449417@gmail.com/我的雲端硬碟/my.aiposter.jp web app for 15s video_換機同步` |
| **Windows 電腦 (預設 G 槽)** | `G:\我的雲端硬碟\my.aiposter.jp web app for 15s video_換機同步` |

---

## 🛠️ 新電腦 (電腦 B) 首次開通步驟

當您第一次在另一台電腦上開啟此專案時，請依序執行以下設定：

1. **確認雲端同步狀態**：
   * 確保電腦上已啟動 **Google 雲端硬碟電腦版** (Google Drive for Desktop)。
   * 確認專案資料夾已完全下載至本機（圖示顯示綠色勾勾）。

2. **開啟 IDE 工作區**：
   * 開啟您的 Antigravity IDE 或 VS Code。
   * 選擇「開啟資料夾」，指向上述的本機同步路徑。

3. **安裝相依套件 (node_modules)**：
   * 專案內已自動排除 `node_modules` 避免雲端同步衝突與上傳過慢。
   * 在專案目錄下開啟終端機，執行以下指令重新安裝套件：
     ```bash
     npm install
     ```

4. **登入 Cloudflare Wrangler**：
   * 如果您需要執行部署或連結雲端 D1 資料庫，請執行：
     ```bash
     npx wrangler login
     ```

---

## 🔄 每日換機交接標準程序 (交接三部曲)

為了避免多台電腦同時修改同一個檔案造成「同步衝突副本」，請在切換電腦時遵循以下交接程序：

### 步驟一：在「準備離開」的電腦上 (例如 Mac A)
1. **儲存所有檔案**：確保編輯器內沒有未存檔的紅色圓點。
2. **關閉所有執行中的伺服器**：
   * 在執行 `npm run dev` 或 `wrangler dev` 的終端機視窗中按下 **`Ctrl + C`** 關閉程序。
   * *理由：關閉伺服器能釋放本機 SQLite 資料表與暫存檔的鎖定，否則 Google Drive 會因為檔案遭系統佔用而無法上傳。*
3. **確認雲端同步完成**：
   * 檢查工作列右下角（或右上角）的 Google Drive 圖示，確認顯示「**同步已完成**」。

### 步驟二：在「準備開始工作」的電腦上 (例如 電腦 B)
1. **確認同步下載**：
   * 開機後，先點擊 Google Drive 圖示，確認背景下載已完成，專案資料夾內已下載另一台電腦剛剛修改的檔案。
2. **開啟專案編輯器**。
3. **執行環境與語法自我檢查**：
   * 在專案終端機中執行：
     ```bash
     npm run check
     ```
   * *如果語法與結構檢查皆顯示 `ok`，代表所有最新修改已安全降落本機！*
4. **啟動本機伺服器繼續開發**：
   ```bash
   npm run dev
   ```

---

## ⚡ 快捷啟動工具

我們在專案中為您新增了自動化啟動腳本，會自動幫您檢查 `node_modules` 是否存在、執行專案健康檢查，並自動開啟本機開發伺服器。

* **Mac/Linux 用戶**：
  直接在專案目錄下執行：
  ```bash
  ./scripts/sync-start.sh
  ```
* **Windows 用戶**：
  在命令提示字元 (cmd) 雙擊或執行：
  ```cmd
  scripts\sync-start.bat
  ```
